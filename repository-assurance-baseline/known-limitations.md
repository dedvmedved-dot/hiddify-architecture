# Known Limitations — RI-01-T00

**Freeze Date:** 2026-07-24T02:06:50Z

## Documented Limitations

### L-01: Deployment Permanently Blocked
The entire lab deployment pipeline is blocked by design. `STAGE06_LAB_DEPLOYMENT_APPROVED != YES`.
No infrastructure has been accessed, no real credentials exist in the repository.

### L-02: Safety Gate Previously Masked
Stage 06 CI workflow previously used `|| true` on the safety gate step (now fixed in Task 1 remediation).
The fix uses `continue-on-error: true` for exit code visibility.

### L-03: Evidence References Stage 05 HEAD
All 43 Stage 06 evidence files reference `51682be` (Stage 05 HEAD), not `e0d80ab` (current HEAD).
This is expected — evidence was generated from Stage 05 baseline and has not been refreshed.

### L-04: No pytest in CI Until Fixed
CI workflow `stage-06-validation.yml` did not install `pytest` until commit `2f3e42f`.
Unit test steps (semantic validator, safety gate) fail in CI without it.

### L-05: Empty Test Placeholders
Several test directories contain only `.gitkeep` files:
- `tests/acceptance/`
- `tests/functional/`
- `tests/integration/`
- `tests/performance/`
- `tests/resilience/`
- `tests/security/`
- `tests/static/`

### L-06: gh CLI Missing read:org Scope
GitHub PAT used for authentication lacks `read:org` scope. Workaround: `GH_TOKEN` env var bypass.

### L-07: No git-lfs
`git-lfs` is not installed but no LFS patterns exist in `.gitattributes`.

### L-08: Manual Evidence Package Creation
Evidence files are created manually (not auto-generated by CI). Timestamps may not reflect actual execution time.

### L-09: No Secret Scanner Binary
`gitleaks` is referenced in `.gitleaksignore` and CI workflow but not installed locally.
CI workflow `secret-scan.yml` handles this.

---
*Generated by Hermes Agent, 2026-07-24T02:06:50Z*
