# Validation Overlap Analysis — RI-01-T00

**Freeze Date:** 2026-07-24T02:06:50Z

## Identified Overlaps

### 1. Evidence Completeness Checking
**Overlapping assets:**
- `validate_stage06_evidence.py` — checks file existence (43 files)
- `validate_stage06_semantic.py` — checks file content (7 semantic checks)

**Overlap:** Both operate on `evidence/stage-06/` files. Evidence validator checks presence; semantic validator checks content. Complementary, not redundant.

### 2. Test ID Validation
**Overlapping assets:**
- `validate_stage06_tests.py` — verifies all 40 ST06-Txxx IDs exist in test plan
- `tests/stage06/test_readiness.py` — executes ST06-T001 through ST06-T040

**Overlap:** Same test IDs. First checks documentation completeness; second executes logical assertions. Complementary.

### 3. Safety Gate vs Owner Input
**Overlapping assets:**
- `stage06_safety_gate.py` — enforces deployment block
- `validate_stage06_owner_input.py` — validates checklist structure

**Overlap:** Both read `owner-input-checklist.md`. Safety gate enforces; validator checks structure. Complementary.

### 4. CI Workflow Steps vs Individual Scripts
**Overlapping assets:**
- `.github/workflows/stage-06-validation.yml` — orchestrates all validators
- Individual `scripts/validate_stage06_*.py` — each a standalone check

**Overlap:** Workflow calls scripts individually. This is intentional orchestration, not redundancy.

## Non-Overlapping Checks (Unique Coverage)

- Markdown lint — unique formatting validation
- ShellCheck — unique shell script analysis
- Secret scan — unique credential detection
- YAML validation — unique YAML syntax check
- Python syntax — unique Python compilation check
- Traceability — unique requirement-to-test mapping
- Stage consistency — unique cross-file stage reference check
- HEAD consistency — unique SHA cross-reference
- Timestamp validation — unique temporal consistency

---
*Generated by Hermes Agent, 2026-07-24T02:06:50Z*
