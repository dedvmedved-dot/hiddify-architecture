---
# Stage 05 — monitoring tasks (CHECK-MODE ONLY)
- name: monitoring — lab validation placeholder
  assert:
    that:
      - lab_environment | default(false)
      - not production_environment | default(true)
  tags: [always, lab, stage05]
