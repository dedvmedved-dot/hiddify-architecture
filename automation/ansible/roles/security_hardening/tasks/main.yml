---
# Stage 05 — security_hardening tasks (CHECK-MODE ONLY)
- name: security_hardening — lab validation placeholder
  assert:
    that:
      - lab_environment | default(false)
      - not production_environment | default(true)
  tags: [always, lab, stage05]
