---
# Stage 05 — system_prerequisites tasks (CHECK-MODE ONLY)
- name: system_prerequisites — lab validation placeholder
  assert:
    that:
      - lab_environment | default(false)
      - not production_environment | default(true)
  tags: [always, lab, stage05]
